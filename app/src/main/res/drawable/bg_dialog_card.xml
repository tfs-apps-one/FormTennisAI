<?xml version="1.0" encoding="utf-8"?>
<!-- Rounded dark card background for the custom "processing" popup (see
     TennisCameraActivity's showProcessingDialog()) — matches the camera
     screen's cyan-on-black HUD styling instead of the plain system
     AlertDialog background the popup used before. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FF14181C" />
    <corners android:radius="20dp" />
    <stroke android:width="1dp" android:color="#3300E5FF" />
</shape>
