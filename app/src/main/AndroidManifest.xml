<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ai_tennis512"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ai_tennis512"
        android:supportsRtl="true"
        android:theme="@style/Theme.FormTennisAI">

        <!-- Launcher: goes straight into the camera diagnosis screen. -->
        <activity
            android:name=".camera.TennisCameraActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".result.TennisResultActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Kept from the project template; no longer the launcher. -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

    </application>

</manifest>
